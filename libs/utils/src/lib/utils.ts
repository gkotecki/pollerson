export enum POLL_SOCKET {
  init = 'poll',
  users = 'users',
  getUsers = 'getUsers',
  setUsers = 'setUsers',
  answers = 'answers',
  getAnswers = 'getAnswers',
  setAnswers = 'setAnswers',
}

export type User = any;

export type Answer = any;
